description = "빌드 실패·에러 로그 진단 (배포 실패 도우미)"
prompt = """
get_project_logs로 {{args}} 프로젝트의 로그를 가져와 GEMINI.md 규칙 기준 대표 실패 유형과 대조 진단하라:
Dockerfile류 존재 / 잘못된 DB 변수명(DB_USERNAME) / 포트·리슨 주소 불일치 / 의존성·메모리·구문 오류.
인증 에러(401)는 배포 실패로 오진하지 말고 MCP 재인증을 안내한다.
원인·수정안·재배포 여부를 한국어로 보고한다. 무료 체험은 빌드 일 5회 제한 — 원인 확인 전 재시도 금지.
"""
