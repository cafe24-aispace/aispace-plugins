description = "AI SPACE MCP 연결 확인·인증 온보딩"
prompt = """
이 확장의 GEMINI.md(운영 지침)를 기준으로 0장 온보딩을 실행하라:
1) AI SPACE MCP 도구(list_my_projects 등) 사용 가능 여부 확인.
2) 미연결이면 이 확장이 이미 서버(https://aih-proxy.cafe24.com/mcp)를 등록하므로, 브라우저 OAuth 인증 1회만 안내한다.
3) 인증 에러 = 로그인 페이지가 아니라 MCP 재인증이 정답.
4) 연결되면 list_my_projects로 현황을 브리핑하고 의도를 묻는다 (새로 만들기 / 만든 것 배포 / 운영·관리).
"""
