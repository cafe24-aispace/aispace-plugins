description = "현재 프로젝트를 AI SPACE에 배포 (배포 전 점검 포함)"
prompt = """
GEMINI.md의 배포 절차를 따르라. 배포 전 점검을 먼저 수행한다:
런타임 지원(Node/Python/PHP/Static — Go·Java·Rust 미지원)·루트 감지 파일·포트(Node 3000/Python 8000, 0.0.0.0)·
DB 하드코딩 제거(자동 주입 변수 DB_USER 등)·영속 경로 /app/user_data/·Dockerfile류 제거.
기존 프로젝트면 get_project_status로 source(internal/github)를 먼저 확인한다.
고친 것/그대로 둔 것을 보여준 뒤 배포하고, get_project_status 폴링으로 진행을 표시하며,
완료 시 site_verify로 검증 후 접속 URL을 보고한다. 같은 에러 2회 연속이면 멈추고 보고한다.
대상: {{args}}
"""
